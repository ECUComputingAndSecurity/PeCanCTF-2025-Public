// _e stnds for "element"

const poc_e = document.getElementById('codeblock');
// const md = markdownit()

// about_e.innerHTML = md.render(about);
// writeup_e.innerHTML = md.render(writeup);
// poc_e.innerHTML = poc

hljs.highlightElement(poc_e);
